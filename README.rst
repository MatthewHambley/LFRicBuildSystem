The ``prepare`` script will create a Python virtual environment and install
Jupyter Notebooks into it.

The ``launch`` script will use the previous prepared environment to run up the
Notebooks server.
